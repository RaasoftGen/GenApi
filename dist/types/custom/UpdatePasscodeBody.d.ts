export declare class UpdatePasscodeBody {
    card_number: string;
    new_passcode: string;
    old_passcode: string;
    constructor(card_number: string, new_passcode: string, old_passcode: string);
}
